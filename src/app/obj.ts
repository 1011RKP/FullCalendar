export class obj {
    title: string;
    start: string;
    end: string;
    backgroundColor: string;
    borderColor: string
    className: string;

    constructor(title: string, start: string, end: string, backgroundColor: string, borderColor: string, className: string) {
        this.title = title;
        this.start = start;
        this.end = end;
        this.backgroundColor = backgroundColor;
        this.borderColor = borderColor;
        this.className = className;
    }
}

export class event {   
    title: string;
    start: string;
    end: string;
    backgroundColor: string;
    borderColor: string
    className: string;
}